#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply
 * Return: the value of the multiplication of x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
